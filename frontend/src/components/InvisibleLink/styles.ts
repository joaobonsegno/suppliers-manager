import styled from 'styled-components';

export const InvisibleLinkContainer = styled.div`
    a {
        display: flex;
        align-items: center;
        justify-content: center;
        
        color: var(--black);
        text-decoration: none;
    }
`;